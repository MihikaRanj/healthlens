import type { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: "com.healthlens.app",
  appName: "HealthLens",
  webDir: "build",
  bundledWebRuntime: false,
};


export default config;
